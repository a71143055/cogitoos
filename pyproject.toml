[project]
name = "cogitoos"
version = "0.1.0"
description = "Intent-aware OS prototype in Python"
authors = [{ name = "Peter", email = "peter@example.com" }]
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["cogitoos"]